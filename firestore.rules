rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Chass! uses the server-side Admin SDK. Browsers never access Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
